# Editable backend configuration (load on startup)
DB_URL=jdbc:postgresql://localhost:5432/SERVI
DB_USER=postgres
DB_PASSWORD=qxnk26yp

JWT_SECRET=
JWT_EXPIRATION_MS=36000000

# Comma-separated allowed origin patterns for CORS
APP_CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:5173,https://*.trycloudflare.com
