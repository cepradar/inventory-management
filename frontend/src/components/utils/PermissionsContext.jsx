/**
 * Re-export de compatibilidad.
 * La implementación real está en src/context/PermissionsContext.jsx
 * Mantiene compatibilidad con imports existentes que usen esta ruta.
 */
export { PermissionsProvider, usePermissions } from '../../context/PermissionsContext';
